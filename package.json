{
  "name": "pacta-ropsten",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pacta-app/pacta-ropsten",
    "create-local": "graph create --node http://localhost:8020/ pacta-app/pacta-ropsten",
    "remove-local": "graph remove --node http://localhost:8020/ pacta-app/pacta-ropsten",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pacta-app/pacta-ropsten"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.17.1",
    "@graphprotocol/graph-ts": "0.17.0"
  }
}
